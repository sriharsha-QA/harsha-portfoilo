export default {
  // Define that the build should be static
  serverBuildTarget: 'static', // This ensures a static build

  // Specify where your static assets should be placed
  publicPath: '/build/',

  // Optionally, configure a custom routes file or asset paths
  // If you want more control over routing and static assets

  // You may also specify other options like the app directory or assets directory
};
